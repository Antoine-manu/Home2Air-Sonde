Class Weather:
	def __init__(self, sensor_id, pressure, temperature, moisture, createdAt)
		self.sensor_id = sensor_id
		self.pressure = pressure
		self.temperature = temperature
		self.moisture = moisture
		selft.createdAt = createdAt

	
	
